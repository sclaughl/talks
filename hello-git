== Basic Git ==
(Start with a cloned repository)

git diff
git add / git rm
git commit
git log (--graph)

== Branching ==
git checkout 
git branch
  * master
  * feature branches
  * release branches
  * git-flow
git rebase (before merge!)
git merge

(gitignore)
(only files are tracked, not dirs)

== Distributed Git ==
repositories
remotes
  origin
  upstream
git push
git fetch
git pull

== Rewriting History ==
git rebase -i

